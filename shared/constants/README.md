### Shared Utilities (`/shared`)

The `/shared` directory contains code and utilities used by both the client and the server (e.g., scoring logic and validation constants).

- `progress.cjs`: Contains shared logic for user progress, including `calculateExerciseXP`.
- `badgeDefinitions.cjs`: Contains badge descriptions for consistent display across client and server.