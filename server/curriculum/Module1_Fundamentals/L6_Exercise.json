{
  "title": "Module 1 Final Challenge: Simple Calculator",
  "instructions": "Build a simple calculator by creating two number variables and performing four arithmetic operations on them. Print all four results.",
  "steps": [
    "Create a variable called `number_a` and assign it any numeric value (integer or float).",
    "Create a variable called `number_b` and assign it any numeric value (integer or float).",
    "Calculate the sum of `number_a` and `number_b`, and store it in `sum_result`. Print the result.",
    "Calculate the difference (`number_a` minus `number_b`), and store it in `difference_result`. Print the result.",
    "Calculate the product (`number_a` times `number_b`), and store it in `product_result`. Print the result.",
    "Calculate the quotient (`number_a` divided by `number_b`), and store it in `quotient_result`. Print the result."
  ],
  "starterCode": "# Step 1: Create your first number variable\n\n\n# Step 2: Create your second number variable\n\n\n# Step 3: Calculate sum\n\n\n# Step 4: Calculate difference\n\n\n# Step 5: Calculate product\n\n\n# Step 6: Calculate quotient\n\n\n",
  "solution": "# Step 1: Create your first number variable\nnumber_a = 50\n\n# Step 2: Create your second number variable\nnumber_b = 7\n\n# Step 3: Calculate sum\nsum_result = number_a + number_b\nprint(sum_result)\n\n# Step 4: Calculate difference\ndifference_result = number_a - number_b\nprint(difference_result)\n\n# Step 5: Calculate product\nproduct_result = number_a * number_b\nprint(product_result)\n\n# Step 6: Calculate quotient\nquotient_result = number_a / number_b\nprint(quotient_result)\n\n",
  "hints": [
    "You can choose any numbers for number_a and number_b - the validation checks that your math is correct, not what numbers you chose!",
    "Remember: sum uses +, difference uses -, product uses *, quotient uses /",
    "Division (/) always returns a float, even if the numbers divide evenly",
    "Make sure to print each result variable on a separate line",
    "Try using different numbers to test your calculator!"
  ],
  "validation": "tests",
  "tests": [
    {
      "name": "Check all required variables exist",
      "code": "# Check that all variables were created\nrequired_vars = ['number_a', 'number_b', 'sum_result', 'difference_result', 'product_result', 'quotient_result']\nmissing = [var for var in required_vars if var not in locals()]\n\nif len(missing) > 0:\n    raise AssertionError(f'Missing variables: {\", \".join(missing)}')\n\nprint('TEST_PASSED: All required variables exist')",
      "expected": "All 6 variables should be created"
    },
    {
      "name": "Check sum_result calculation",
      "code": "# Verify sum is correct\nexpected_sum = number_a + number_b\n\nif sum_result != expected_sum:\n    raise AssertionError(f'sum_result should equal number_a + number_b ({number_a} + {number_b} = {expected_sum}), got: {sum_result}')\n\nprint('TEST_PASSED: sum_result calculation is correct')",
      "expected": "sum_result = number_a + number_b"
    },
    {
      "name": "Check difference_result calculation",
      "code": "# Verify difference is correct\nexpected_diff = number_a - number_b\n\nif difference_result != expected_diff:\n    raise AssertionError(f'difference_result should equal number_a - number_b ({number_a} - {number_b} = {expected_diff}), got: {difference_result}')\n\nprint('TEST_PASSED: difference_result calculation is correct')",
      "expected": "difference_result = number_a - number_b"
    },
    {
      "name": "Check product_result calculation",
      "code": "# Verify product is correct\nexpected_product = number_a * number_b\n\nif product_result != expected_product:\n    raise AssertionError(f'product_result should equal number_a * number_b ({number_a} * {number_b} = {expected_product}), got: {product_result}')\n\nprint('TEST_PASSED: product_result calculation is correct')",
      "expected": "product_result = number_a * number_b"
    },
    {
      "name": "Check quotient_result calculation",
      "code": "# Verify quotient is correct\nexpected_quotient = number_a / number_b\n\nif quotient_result != expected_quotient:\n    raise AssertionError(f'quotient_result should equal number_a / number_b ({number_a} / {number_b} = {expected_quotient}), got: {quotient_result}')\n\nprint('TEST_PASSED: quotient_result calculation is correct')",
      "expected": "quotient_result = number_a / number_b"
    },
    {
      "name": "Check output has 4 lines",
      "code": "import io\nimport sys\n\n# Capture output by re-executing\nold_stdout = sys.stdout\nsys.stdout = io.StringIO()\n\ntry:\n    exec(code)\n    output = sys.stdout.getvalue().strip()\nfinally:\n    sys.stdout = old_stdout\n\nlines = [line for line in output.split('\\n') if line.strip()]\n\nif len(lines) < 4:\n    raise AssertionError(f'Should print at least 4 results (one per line), got {len(lines)} lines')\n\nprint('TEST_PASSED: Output has sufficient lines')",
      "expected": "At least 4 lines of output"
    }
  ],
  "tags": ["project", "calculator", "module-review", "variables", "operators"],
  "challengeGroup": "module-1-final"
}
