{
  "title": "Student Grade Calculator",
  "instructions": "You're building a grade calculator for a teacher. Write an `if-elif-else` chain that checks a student's score and prints their letter grade based on the following scale:\n\n| Score Range | Grade |\n| :--- | :--- |\n| 90 or higher | A |\n| 80 to 89 | B |\n| 70 to 79 | C |\n| 60 to 69 | D |\n| Below 60 | F |\n\nRemember: Python checks conditions from top to bottom and stops at the first `True` condition!",
  "steps": [
    "**Step 1:** Create an `if` statement to check for a score of 90 or higher and print `'Grade: A'`.",
    "**Step 2:** Use `elif` statements to check for scores of 80, 70, and 60 respectively, printing the correct grade for each.",
    "**Step 3:** Add a final `else` statement to catch any scores below 60 and print `'Grade: F'`."
  ],
  "starterCode": "# Student's test score\nstudent_score = 75\n\nprint(f\"Student scored: {student_score}\")\n\n# Write your if-elif-else chain below:\n",
  "solution": "# Student's test score\nstudent_score = 75\n\nprint(f\"Student scored: {student_score}\")\n\nif student_score >= 90:\n    print('Grade: A')\nelif student_score >= 80:\n    print('Grade: B')\nelif student_score >= 70:\n    print('Grade: C')\nelif student_score >= 60:\n    print('Grade: D')\nelse:\n    print('Grade: F')",
  "hints": [
    "**Syntax reminder:** Every `if`, `elif`, and `else` statement must end with a colon `:` and the code below must be indented.",
    "**Order matters!** Python stops at the first `True` match. If you check `>= 60` first, a score of 95 would trigger that and stop early.",
    "**The >= operator:** This means 'greater than OR equal to'.",
    "**No condition for else:** The `else` block is a catch-all and doesn't need its own comparison."
  ],
  "validation": "tests",
  "tests": [
    {
      "name": "Verify all grade boundaries",
      "code": "import io, sys\n# Test cases to verify the logic\nscores_to_test = [(95, 'A'), (85, 'B'), (75, 'C'), (65, 'D'), (55, 'F')]\n\nfor score, expected_grade in scores_to_test:\n    old_stdout = sys.stdout\n    sys.stdout = io.StringIO()\n    try:\n        test_code = code.replace('student_score = 75', f'student_score = {score}')\n        exec(test_code, {})\n        output = sys.stdout.getvalue().strip().lower()\n    finally:\n        sys.stdout = old_stdout\n    \n    # Flexible check: ensures 'grade' and the letter are present\n    if 'grade' not in output or expected_grade.lower() not in output:\n        raise AssertionError(f'Testing score {score}: We couldn\\'t find \"Grade {expected_grade}\" in your output. Make sure you are printing the grade!')\n\nprint('TEST_PASSED: All grade boundaries work correctly')",
      "expected": "The calculator should identify the correct letter grade regardless of colons or capitalization."
    },
    {
      "name": "Check for proper if-elif-else structure",
      "code": "import ast\ntree = ast.parse(code)\nif_nodes = [n for n in ast.walk(tree) if isinstance(n, ast.If)]\n\nif not if_nodes:\n    raise AssertionError('You must use an if-elif-else structure.')\n\n# Ensure they used elifs by checking the 'orelse' nesting\nelif_count = 0\ncurr = if_nodes[0]\nwhile curr.orelse and isinstance(curr.orelse[0], ast.If):\n    elif_count += 1\n    curr = curr.orelse[0]\n\nif elif_count < 3:\n    raise AssertionError(f'You should use several \"elif\" statements to check each score range. Found {elif_count} elifs.')\n\nprint('TEST_PASSED: Logic structure is correct')",
      "expected": "Code should utilize the if-elif-else pattern as requested."
    }
  ],
  "expectedOutput": "Student scored: 75\nGrade: C",
  "tags": ["if", "elif", "else", "control-flow", "conditionals"],
  "challengeGroup": "conditionals-basics"
}
