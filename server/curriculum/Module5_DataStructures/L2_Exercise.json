{
  "title": "Dictionary Manipulation & Loop",
  "instructions": "Practice adding, removing, and iterating over dictionaries using `.pop()`, `.values()`, and `.items()`.",
  "steps": [
    "Task 1: Add 'status': 'online' to `user_data`",
    "Task 2: Remove 'temp_id' using `.pop()` and store its value in `removed_id`",
    "Task 3: Loop through `item_prices.values()` to calculate total price",
    "Task 4: Loop through `user_data.items()` to print each key-value pair"
  ],
  "starterCode": "user_data = {'username': 'test_user', 'level': 10, 'temp_id': 999}\nitem_prices = {'apple': 0.50, 'banana': 0.30, 'kiwi': 0.75}\ntotal_price = 0\n\n# Task 1: Add status\n\n# Task 2: Remove temp_id and store value\nremoved_id = \n\n# Task 3: Calculate total price\n\n# Task 4: Print all key-value pairs\n",
  "solution": "user_data['status'] = 'online'\nremoved_id = user_data.pop('temp_id')\n\nfor price in item_prices.values():\n    total_price += price\n\nfor key, value in user_data.items():\n    print(f'Key: {key}, Value: {value}')",
  "hints": [
    "Task 1: Add key-value: `user_data['status'] = 'online'`",
    "Task 2: `.pop()` removes and returns: `user_data.pop('temp_id')`",
    "Task 3: `.values()` gives only values: `for price in item_prices.values():`",
    "Task 4: `.items()` gives (key, value) pairs: `for key, value in user_data.items():`",
    "Note: `.pop()` modifies the dictionary and returns the removed value",
    "F-strings make formatting easy: `f'Key: {key}, Value: {value}'`"
  ],
  "validation": "tests",
  "tests": [
    {
      "name": "Check dictionary operations",
      "code": "import io, sys\nold_stdout = sys.stdout\nsys.stdout = io.StringIO()\nexec_globals = {}\nexec(code, exec_globals)\noutput = sys.stdout.getvalue()\nsys.stdout = old_stdout\n\nud = exec_globals.get('user_data', {})\nif 'temp_id' in ud:\n    raise AssertionError('Task 2: temp_id should be removed with .pop()')\n\nif ud.get('status') != 'online':\n    raise AssertionError(\"Task 1: 'status' should be 'online'\")\n\nif abs(exec_globals.get('total_price', 0) - 1.55) > 0.001:\n    raise AssertionError(f'Task 3: total_price should be 1.55, got {exec_globals.get(\"total_price\")}')\n\nif 'Key: username, Value: test_user' not in output:\n    raise AssertionError('Task 4: Should print all key-value pairs using .items()')\n\nprint('✓ Dictionary manipulation successful!')"
    }
  ],
  "tags": ["pop", "items", "values", "iteration", "beginner"]
}
