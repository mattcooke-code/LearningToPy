{
  "title": "Module 13 Project: Event Timer and Date Validator",
  "instructions": "Build two practical date utilities: an event timer that calculates time remaining until a future date, and a date validator that safely checks if a date string matches a given format.",
  "steps": [
    "**Task 1: Import required modules**\n* Import `datetime` from the `datetime` module.",
    "**Task 2: Define the Event Timer function**\nCreate a function called `time_until_event(event_date_str)` that:\n* Defines a constant `DATE_FORMAT = '%Y-%m-%d'`.\n* Parses `event_date_str` into a datetime object using `strptime()`.\n* Gets the current datetime using `datetime.now()`.\n* Calculates the difference between the event date and the current time.\n* Returns `'Event has already passed!'` if the difference is negative.\n* Otherwise, extracts the days, hours, and minutes from the timedelta.\n* Returns a formatted string: `'{days} days, {hours} hours, {minutes} minutes remaining.'`",
    "**Task 3: Define the Date Validator function**\nCreate a function called `is_valid_date_format(date_str, format_str)` that:\n* Uses a `try` block to attempt `datetime.strptime(date_str, format_str)`.\n* Returns `True` if the parsing is successful.\n* Returns `False` in an `except ValueError` block if parsing fails.",
    "**Task 4: Test your functions**\n* Call `time_until_event('2030-01-01')` and print the result.\n* Call `is_valid_date_format('12/25/2025', '%m/%d/%Y')` and print the result.\n* Call `is_valid_date_format('Dec 25 2025', '%m/%d/%Y')` and print the result."
  ],
  "starterCode": "# Task 1: Import datetime\n\n\n\n# Task 2: Define time_until_event function\n\n\n\n# Task 3: Define is_valid_date_format function\n\n\n\n# Task 4: Test your functions\nfuture_event = '2030-01-01'\nprint(f\"Time until {future_event}: {time_until_event(future_event)}\")\n\nprint(f\"Valid (12/25/2025, %m/%d/%Y): {is_valid_date_format('12/25/2025', '%m/%d/%Y')}\")\nprint(f\"Invalid (Dec 25 2025, %m/%d/%Y): {is_valid_date_format('Dec 25 2025', '%m/%d/%Y')}\")\n",
  "solution": "# Task 1: Import datetime\nfrom datetime import datetime\n\n# Task 2: Define time_until_event function\ndef time_until_event(event_date_str):\n    DATE_FORMAT = '%Y-%m-%d'\n    \n    # Parse the future date\n    event_date = datetime.strptime(event_date_str, DATE_FORMAT)\n    \n    # Get current datetime\n    now = datetime.now()\n    \n    # Calculate difference\n    time_remaining = event_date - now\n    \n    # Check if event has passed\n    if time_remaining.total_seconds() < 0:\n        return \"Event has already passed!\"\n    \n    # Extract days, hours, minutes\n    total_seconds = time_remaining.total_seconds()\n    \n    days = int(total_seconds // (24 * 3600))\n    remaining_seconds = total_seconds % (24 * 3600)\n    hours = int(remaining_seconds // 3600)\n    minutes = int((remaining_seconds % 3600) // 60)\n    \n    return f\"{days} days, {hours} hours, {minutes} minutes remaining.\"\n\n# Task 3: Define is_valid_date_format function\ndef is_valid_date_format(date_str, format_str):\n    try:\n        datetime.strptime(date_str, format_str)\n        return True\n    except ValueError:\n        return False\n\n# Task 4: Test your functions\nfuture_event = '2030-01-01'\nprint(f\"Time until {future_event}: {time_until_event(future_event)}\")\n\nprint(f\"Valid (12/25/2025, %m/%d/%Y): {is_valid_date_format('12/25/2025', '%m/%d/%Y')}\")\nprint(f\"Invalid (Dec 25 2025, %m/%d/%Y): {is_valid_date_format('Dec 25 2025', '%m/%d/%Y')}\")\n",
  "fileSetup": {},
  "hints": [
    "**Task 1: Import**\n```python\nfrom datetime import datetime\n```\n\n**Task 2: Event Timer calculations**\n```python\ndef time_until_event(event_date_str):\n    DATE_FORMAT = '%Y-%m-%d'\n    event_date = datetime.strptime(event_date_str, DATE_FORMAT)\n    now = datetime.now()\n    time_remaining = event_date - now\n    \n    if time_remaining.total_seconds() < 0:\n        return \"Event has already passed!\"\n    \n    # Extract components\n    total_seconds = time_remaining.total_seconds()\n    days = int(total_seconds // (24 * 3600))\n    remaining = total_seconds % (24 * 3600)\n    hours = int(remaining // 3600)\n    minutes = int((remaining % 3600) // 60)\n    \n    return f\"{days} days, {hours} hours, {minutes} minutes remaining.\"\n```\n\n**Task 3: Date Validator**\n```python\ndef is_valid_date_format(date_str, format_str):\n    try:\n        datetime.strptime(date_str, format_str)\n        return True\n    except ValueError:\n        return False\n```\n\n**Task 4: Testing**\n```python\nprint(time_until_event('2030-01-01'))\nprint(is_valid_date_format('12/25/2025', '%m/%d/%Y'))  # True\nprint(is_valid_date_format('Dec 25 2025', '%m/%d/%Y'))  # False\n```\n\n**Key Formulas:**\n- Seconds in a day: `24 * 3600 = 86400`\n- Hours from remaining seconds: `remaining_seconds // 3600`\n- Minutes from remaining seconds: `(remaining_seconds % 3600) // 60`"
  ],
  "validation": "tests",
  "tests": [
    {
      "name": "Check import and function definitions",
      "code": "import ast\n\n# Parse the code\nuser_code = code\ntree = ast.parse(user_code)\n\n# Check import\nfound_import = False\nfor node in ast.walk(tree):\n    if isinstance(node, ast.ImportFrom) and node.module == 'datetime':\n        for name in node.names:\n            if name.name == 'datetime':\n                found_import = True\n\nif not found_import:\n    raise AssertionError('Task 1: Missing import: from datetime import datetime')\n\n# Check function definitions\nfunctions = [node.name for node in ast.walk(tree) if isinstance(node, ast.FunctionDef)]\n\nif 'time_until_event' not in functions:\n    raise AssertionError('Task 2: time_until_event function not defined')\nif 'is_valid_date_format' not in functions:\n    raise AssertionError('Task 3: is_valid_date_format function not defined')\n\nprint('TEST_PASSED')"
    },
    {
      "name": "Check is_valid_date_format logic",
      "code": "from datetime import datetime\n\n# Parse the code to extract function\nuser_code = code\nexec_globals = {}\nexec(user_code, exec_globals)\n\nif 'is_valid_date_format' not in exec_globals:\n    raise AssertionError('is_valid_date_format function not found')\n\nvalidate = exec_globals['is_valid_date_format']\n\n# Test valid case\nif not validate('12/25/2025', '%m/%d/%Y'):\n    raise AssertionError('Task 3: validate(\"12/25/2025\", \"%m/%d/%Y\") should return True')\n\n# Test invalid case\nif validate('Dec 25 2025', '%m/%d/%Y'):\n    raise AssertionError('Task 3: validate(\"Dec 25 2025\", \"%m/%d/%Y\") should return False')\n\n# Test invalid date (Feb 30)\nif validate('02/30/2025', '%m/%d/%Y'):\n    raise AssertionError('Task 3: validate(\"02/30/2025\", \"%m/%d/%Y\") should return False (invalid date)')\n\nprint('TEST_PASSED')"
    },
    {
      "name": "Check time_until_event structure",
      "code": "import ast\n\n# Parse the code\nuser_code = code\ntree = ast.parse(user_code)\n\n# Find time_until_event function and check for key elements\nfor node in ast.walk(tree):\n    if isinstance(node, ast.FunctionDef) and node.name == 'time_until_event':\n        # Check for datetime.strptime call\n        has_strptime = False\n        has_now = False\n        has_return = False\n        \n        for subnode in ast.walk(node):\n            if isinstance(subnode, ast.Call):\n                if isinstance(subnode.func, ast.Attribute):\n                    if subnode.func.attr == 'strptime':\n                        has_strptime = True\n                    if subnode.func.attr == 'now':\n                        has_now = True\n            if isinstance(subnode, ast.Return):\n                has_return = True\n        \n        if not has_strptime:\n            raise AssertionError('Task 2: time_until_event should use datetime.strptime()')\n        if not has_now:\n            raise AssertionError('Task 2: time_until_event should use datetime.now()')\n        if not has_return:\n            raise AssertionError('Task 2: time_until_event should return a value')\n        \n        break\n\nprint('TEST_PASSED')"
    },
    {
      "name": "Check output format",
      "code": "import sys\nfrom io import StringIO\nfrom datetime import datetime\n\n# Capture output\nold_stdout = sys.stdout\nsys.stdout = StringIO()\n\ntry:\n    exec_globals = {}\n    exec(code, exec_globals)\n    output = sys.stdout.getvalue()\nfinally:\n    sys.stdout = old_stdout\n\nlines = [line.strip() for line in output.strip().split('\\n') if line.strip()]\n\n# Should have at least 3 lines of output\nif len(lines) < 3:\n    raise AssertionError('Task 4: Expected at least 3 lines of output')\n\n# Check validation outputs\nif 'True' not in lines[1] and 'True' not in lines[1]:\n    raise AssertionError('Task 4: Second line should show True for valid date')\nif 'False' not in lines[2] and 'False' not in lines[2]:\n    raise AssertionError('Task 4: Third line should show False for invalid date')\n\nprint('TEST_PASSED')"
    }
  ],
  "tags": ["project", "datetime", "timedelta", "strptime", "try-except"],
  "challengeGroup": "datetime-project"
}
