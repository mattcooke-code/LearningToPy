{
  "lessonId": "M8L2",
  "title": "Handling Specific Exceptions - Quiz",
  "questions": [
    {
      "id": "m8l2_q1",
      "type": "multiple-choice",
      "question": "Which exception would be raised if you tried to access `my_list[5]` on a list that only has 3 items?",
      "options": ["`ValueError`", "`KeyError`", "`IndexError`", "`TypeError`"],
      "correctAnswer": 2,
      "explanation": "An `IndexError` occurs when you try to access an index that is outside the bounds of a list or tuple."
    },
    {
      "id": "m8l2_q2",
      "type": "multiple-choice",
      "question": "When using multiple `except` blocks, why does the order matter?",
      "options": [
        "Python only runs the last `except` block in the list",
        "Python runs the first `except` block that matches the error type and ignores the rest",
        "Python runs all `except` blocks that could possibly match",
        "The most general exception must always come first"
      ],
      "correctAnswer": 1,
      "explanation": "Python stops at the first matching `except` block. If you put a very general exception first, the more specific ones below it will never run."
    },
    {
      "id": "m8l2_q3",
      "type": "multiple-choice",
      "question": "In the statement `except ValueError as e:`, what does the `e` represent?",
      "options": [
        "The word 'Error'",
        "An empty variable to clear the error",
        "An object containing the specific details/message of the exception",
        "A command to exit the program"
      ],
      "correctAnswer": 2,
      "explanation": "The `as` keyword captures the exception object, allowing you to print the error message generated by Python (e.g., why a value was invalid)."
    }
  ]
}
