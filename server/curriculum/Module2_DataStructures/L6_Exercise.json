{
  "title": "Module 2 Project: Inventory Manager",
  "instructions": "Congratulations on reaching the Module 2 project! You're building a simple retail inventory system. You'll use a LIST to manage items in stock (which changes constantly) and a TUPLE to track stock history (which should never change). Complete all 5 steps to simulate a busy day at the shop!",
  "steps": [
    "**Step 1: New Shipment Arrives** - A delivery truck just arrived! Use the `append()` method to add 'Water Bottle' to the end of the `inventory_items` list.",
    "**Step 2: Found Misplaced Item** - Good news! The 'Notebook' that was missing has been found and doesn't need to be in the inventory. Use the `remove()` method to delete 'Notebook' from the list.",
    "**Step 3: First Sale of the Day** - A customer wants to buy the oldest item in stock (the first one in the list). Use the `pop()` method with index 0 to remove it and store it in the variable `sold_item` so you can print a receipt.",
    "**Step 4: Manager's Quick Check** - The manager wants to see which items will likely sell next. Use slicing to get the 2nd and 3rd items in the current inventory (indices 1 and 2). Store this slice in `next_to_be_sold`.",
    "**Step 5: Yesterday's Report** - Check the stock history to see how many items you had yesterday. Use negative indexing to get the last number from the `daily_stock_history` tuple and store it in `yesterdays_count`."
  ],
  "starterCode": "# Current items in the shop (this list will change!)\ninventory_items = ['T-Shirt', 'Pants', 'Socks', 'Notebook', 'Pen']\n\n# Historical record of daily stock counts (this should never change!)\ndaily_stock_history = (150, 155, 148, 160)\n\nprint(\"📦 STARTING INVENTORY MANAGER\")\nprint(\"Starting inventory:\", inventory_items)\nprint(\"Stock history:\", daily_stock_history)\nprint()\n\n# Step 1: Add 'Water Bottle' to the end of inventory\n\n\nprint(\"After new shipment:\", inventory_items)\n\n# Step 2: Remove 'Notebook' by value\n\n\nprint(\"After removing misplaced item:\", inventory_items)\n\n# Step 3: Sell the first item (pop index 0) and save it\nsold_item = \n\nprint(f\"After sale - Sold: {sold_item}\")\nprint(\"Remaining inventory:\", inventory_items)\n\n# Step 4: Slice items at indices 1 and 2 (remember: stop is exclusive!)\nnext_to_be_sold = \n\nprint(\"Next likely to sell:\", next_to_be_sold)\n\n# Step 5: Get the last value from the history tuple\nyesterdays_count = \n\nprint(f\"Yesterday's total stock: {yesterdays_count}\")\nprint()\nprint(\"📊 FINAL REPORT\")\nprint(f\"Current inventory: {inventory_items}\")\nprint(f\"Items in stock: {len(inventory_items)}\")",
  "solution": "# Current items in the shop (this list will change!)\ninventory_items = ['T-Shirt', 'Pants', 'Socks', 'Notebook', 'Pen']\n\n# Historical record of daily stock counts (this should never change!)\ndaily_stock_history = (150, 155, 148, 160)\n\nprint(\"📦 STARTING INVENTORY MANAGER\")\nprint(\"Starting inventory:\", inventory_items)\nprint(\"Stock history:\", daily_stock_history)\nprint()\n\n# Step 1: Add 'Water Bottle' to the end of inventory\ninventory_items.append('Water Bottle')\n\nprint(\"After new shipment:\", inventory_items)\n\n# Step 2: Remove 'Notebook' by value\ninventory_items.remove('Notebook')\n\nprint(\"After removing misplaced item:\", inventory_items)\n\n# Step 3: Sell the first item (pop index 0) and save it\nsold_item = inventory_items.pop(0)\n\nprint(f\"After sale - Sold: {sold_item}\")\nprint(\"Remaining inventory:\", inventory_items)\n\n# Step 4: Slice items at indices 1 and 2 (remember: stop is exclusive!)\nnext_to_be_sold = inventory_items[1:3]\n\nprint(\"Next likely to sell:\", next_to_be_sold)\n\n# Step 5: Get the last value from the history tuple\nyesterdays_count = daily_stock_history[-1]\n\nprint(f\"Yesterday's total stock: {yesterdays_count}\")\nprint()\nprint(\"📊 FINAL REPORT\")\nprint(f\"Current inventory: {inventory_items}\")\nprint(f\"Items in stock: {len(inventory_items)}\")",
  "hints": [
    "**Step 1 - Adding items:** To add something to the very end of a list, use the `.append()` method. Example: `my_list.append('new_item')`.",
    "**Step 2 - Deleting by name:** If you know the name of the item but not its position, use `.remove()`. Remember to put the item name in quotes inside the parentheses!",
    "**Step 3 - Popping indices:** The `.pop()` method removes an item and 'hands it back' to you. To capture that item, you need to assign it to a variable: `item = my_list.pop(index)`.",
    "**Step 4 - Slicing Logic:** Remember that `[start:stop]` is inclusive of the start but **exclusive** of the stop. If you want the item at index 2 to be included, your 'stop' number needs to be 3.",
    "**Step 5 - The Tail End:** In Python, you don't need to know the length of a tuple to get the last item. Negative indexing starts at `-1` for the very last element.",
    "**Mutability Check:** If you get an error trying to change the history, remember: Lists are like whiteboards (writable), but Tuples are like stone tablets (immutable).",
    "**Debugging Tip:** If your inventory looks wrong, check the order of your steps. Each step modifies the list that the next step then uses!"
  ],
  "validation": "tests",
  "tests": [
    {
      "name": "Validate Inventory Manager Logic and Methods",
      "code": "import ast\n# 1. Logic Check (Final State)\nif sold_item != 'T-Shirt':\n    raise AssertionError('sold_item should be \"T-Shirt\" (the first item removed).')\nif next_to_be_sold != ['Socks', 'Pen']:\n    raise AssertionError(f'next_to_be_sold slice is incorrect. Got: {next_to_be_sold}')\nif yesterdays_count != 160:\n    raise AssertionError('yesterdays_count should be 160 (the last item in the history tuple).')\nif inventory_items != ['Pants', 'Socks', 'Pen', 'Water Bottle']:\n    raise AssertionError(f'Final inventory is incorrect. Got: {inventory_items}')\n\n# 2. Syntax Check (Ensuring specific methods were used)\ntree = ast.parse(code)\ncalls = [node.func.attr for node in ast.walk(tree) if isinstance(node, ast.Call) and isinstance(node.func, ast.Attribute)]\n\nif 'append' not in calls:\n    raise AssertionError('Step 1: You must use the .append() method.')\nif 'remove' not in calls:\n    raise AssertionError('Step 2: You must use the .remove() method.')\nif 'pop' not in calls:\n    raise AssertionError('Step 3: You must use the .pop(0) method.')\n\nprint('TEST_PASSED: Inventory Manager project completed successfully!')",
      "expected": "All inventory operations (append, remove, pop, slice) performed correctly."
    }
  ],
  "expectedOutput": "📦 STARTING INVENTORY MANAGER\nStarting inventory: ['T-Shirt', 'Pants', 'Socks', 'Notebook', 'Pen']\nStock history: (150, 155, 148, 160)\n\nAfter new shipment: ['T-Shirt', 'Pants', 'Socks', 'Notebook', 'Pen', 'Water Bottle']\nAfter removing misplaced item: ['T-Shirt', 'Pants', 'Socks', 'Pen', 'Water Bottle']\nAfter sale - Sold: T-Shirt\nRemaining inventory: ['Pants', 'Socks', 'Pen', 'Water Bottle']\nNext likely to sell: ['Socks', 'Pen']\nYesterday's total stock: 160\n\n📊 FINAL REPORT\nCurrent inventory: ['Pants', 'Socks', 'Pen', 'Water Bottle']\nItems in stock: 4",
  "tags": [
    "project",
    "lists",
    "tuples",
    "mutability",
    "immutability",
    "module-2"
  ],
  "challengeGroup": "module-2-project"
}
